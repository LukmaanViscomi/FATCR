###########################################################
# Generic Python / Streamlit project ignores
###########################################################

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.dll

# Virtual environments
.venv/
venv/
env/
ENV/

# Build artifacts
build/
dist/
*.egg-info/

# Jupyter
.ipynb_checkpoints/
*.nbconvert.ipynb

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*.tmp
*.bak
*.orig

# IDE settings
.vscode/
.idea/
*.code-workspace

# Logs / caches
logs/
log/
*.log
tmp/
temp/
.cache/
*.pid



###########################################################
# FACTR-specific data and artefacts
###########################################################

# Raw data is always generated / large
data/raw/

# Keep all processed outputs, but exclude ONLY the two largest indices
data/processed/KB_all.faiss
data/processed/KB_islam.faiss

# Keep KB_*.map.jsonl (do NOT ignore them)

# Keep snapshots (do NOT ignore snapshots/)

# Logs / runtime noise (optional – keep if you want logs out of git)
data/processed/*.log

# Never commit cookies/secrets
**/yt_cookies.txt

# Never commit model caches
models/

# Local-only config / secrets (never commit)
.streamlit/secrets.toml
*.env
.env
.env.*

# Snapshots: keep only the 3 pointer files
snapshots/*
!snapshots/ASR_LAST.json
!snapshots/CLAIMS_LAST.json
!snapshots/VERIFY_LAST.json
