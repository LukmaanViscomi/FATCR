###########################################################
# Generic Python / Streamlit project ignores
###########################################################

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.dll

# Virtual environments
.venv/
venv/
env/
ENV/

# Build artifacts
build/
dist/
*.egg-info/

# Jupyter
.ipynb_checkpoints/
*.nbconvert.ipynb

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*.tmp
*.bak
*.orig

# IDE settings
.vscode/
.idea/
*.code-workspace

# Logs / caches
logs/
log/
*.log
tmp/
temp/
.cache/
*.pid



###########################################################
# FACTR-specific data and artefacts
###########################################################

# Raw data is always generated / large
data/raw/

# Track data/processed by default — but exclude the big/generated artefacts
# (keep small JSON/CSV/Parquet snapshots & evaluation outputs in git)

# --- Exclude large FAISS indices ---
data/processed/KB_all.faiss
data/processed/KB_islam.faiss
data/processed/KB_christian.faiss

# Optional: if this one is also big for you, exclude it too
# data/processed/KB.faiss

# --- Exclude large / regenerable mapping files (enable if needed) ---
# If you DO want to keep these, comment these 3 lines out.
data/processed/KB_all.map.jsonl
data/processed/KB_islam.map.jsonl
data/processed/KB_christian.map.jsonl

# Optional: passages file is often huge
data/processed/KB_passages.jsonl

# Logs / local runtime files
data/processed/*.log

# Never commit cookies/secrets
**/yt_cookies.txt

# Never commit model caches
models/

# Local-only config / secrets (never commit)
.streamlit/secrets.toml
*.env
.env
.env.*

